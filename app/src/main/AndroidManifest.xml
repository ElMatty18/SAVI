<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".SaviApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_casa"
        android:supportsRtl="true"
        android:theme="@style/Theme.Savi">
        <activity
            android:name=".ui.acceso.AccesoActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi.Splash"
            android:windowSoftInputMode="adjustResize" />

        <!-- Se conserva el nombre del launcher original para no romper accesos directos -->
        <activity-alias
            android:name=".Activitys.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:targetActivity=".ui.acceso.AccesoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- singleTask: las pantallas viejas vuelven al inicio con startActivity y se apilaban copias -->
        <activity
            android:name=".ui.inicio.InicioActivity"
            android:label="@string/title_activity_menu_principal"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.alertas.DetalleAlertaActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />

        <activity
            android:name=".ui.comun.SelectorUbicacionActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.grupo.CrearGrupoActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.grupo.GrupoActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.notificaciones.NotificacionesActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.notificaciones.NuevaNotificacionActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.perfil.DomiciliosActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.configuracion.RespuestaAutomaticaActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.informacion.InformacionActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.perfil.PerfilActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.familia.FamiliaActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.alertas.AlertasActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />
        <activity
            android:name=".ui.alertas.EmitirAlertaActivity"
            android:exported="false"
            android:theme="@style/Theme.Savi" />

        <!-- La key de Maps se inyecta desde local.properties (MAPS_API_KEY), ver README. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- service -->

        <service android:name=".Service.AlertaService" android:exported="false" />
        <service android:name=".Service.NotificacionService" android:exported="false" />

        <!-- fin -->

        <service
            android:name=".Service.SaviMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>